{% if pagelist -%}
  <ul class="columns is-multiline mx-0">
    {%- for post in pagelist -%}
      <li class="column is-one-third">
        {%- preview post.data.preview, post.data.title, page=post -%}
        <h3 class="title">
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h3>
        {%- if post.dateCreated -%}
          <p class="subtitle">
            <time datetime="{{ post.dateCreated | dateymd }}">{{ post.dateCreated | datefriendly }}</time>
          </p>
        {%- endif -%}
        <p>{% excerpt post %}</p>
      </li>
    {%- endfor -%}
  </ul>
{%- endif %}